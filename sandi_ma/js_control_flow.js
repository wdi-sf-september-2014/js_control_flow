var base = prompt("Please enter a base number");
var expon = prompt("Please enter the exponent");

var calculateExponent = function(base, expon) {
    console.log("Your answer is " + Math.pow(base, expon));
    
}
calculateExponent(base, expon);

//Your code here
